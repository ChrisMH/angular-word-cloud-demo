import { WordCloudDirective } from './word-cloud.directive';

describe('AwcWordCloudDirective', () => {
  it('should create an instance', () => {
    const directive = new AwcWordCloudDirective();
    expect(directive).toBeTruthy();
  });
});
